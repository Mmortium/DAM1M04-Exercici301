<!DOCTYPE html>
<html lang="ca">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/estils.css">
</head>
<body>
    {{> header}} {{!-- Esto carga el menú del partial --}}

    <h1>{{title}}</h1>
    <p>{{description}}</p>

    {{#if showWarning}}
        <div style="background-color: #ffcccc; padding: 15px; border: 1px solid red;">
            <strong>Avís:</strong> {{warningMessage}}
        </div>
    {{/if}}

    <footer style="margin-top: 50px; font-size: 0.8em;">
        {{footer.text}} - Versió: {{footer.version}}
    </footer>
</body>
</html>